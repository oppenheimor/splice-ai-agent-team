# 2026-05-28 工作日志

## 架构文档评审修正（第二轮）
- 修正 API 命名不一致：3处旧引用（/enhance × 2 + /sync × 1）统一为 /complete
- 新增叙事增强失败降级策略：报告骨架永不阻塞，4种降级场景 + 重试逻辑
- 确认命名规范：Agent 标识符统一用 `requirements-diagnosis`（复数），不用单数 `requirement-diagnosis`
  - 理由：复数更自然（类比 requirements engineering），且与现有代码（registry/proxy/prompt 文件名）一致
  - 在架构文档头部添加了命名规范声明
