# Product

## Register

product

## Users

这是一个可持续扩展的多 Agent 产品平台。平台本身服务于 Agent 产品的搭建、访问和复用，核心用户既包括平台建设者，也包括进入具体 Agent 完成任务的终端用户。

不同 Agent 面向不同人群，需要保留各自的领域体验：

- 寻宝游戏 Agent 面向需要设计礼物寻宝游戏的用户，例如宝妈、情侣、朋友或活动组织者；他们需要一个轻松、有想象力、能把创意落到执行清单的体验。
- 需求诊断 Agent 面向正在考虑 AI 转型的老板、创业者或业务负责人；他们需要一个可信、清晰、能帮助他们看见现状并形成行动路径的诊断体验。
- 后续新增 Agent 可能拥有新的用户群体、任务节奏和界面风格，但底层聊天、AGUI 工具、会话和运行时能力应尽量复用。

## Product Purpose

Splice Agent Team 的目标是让多个 Agent 共用稳定的聊天与工具交互底座，同时让每个 Agent 都拥有独立页面、对话能力和差异化交互 UI。

成功的产品体验不是“同一个聊天框换提示词”，而是每个 Agent 都能围绕自身业务目标形成合适的交互流程、视觉语气和结果呈现方式。平台层应降低新增 Agent 的成本，Agent 层应让用户感觉它是为当前任务专门设计的。

## Brand Personality

平台层气质：克制、专业、可扩展。

Agent 层气质：根据目标用户和任务场景具体分析。寻宝游戏 Agent 可以更温暖、轻盈、富有仪式感；需求诊断 Agent 应更可信、清醒、有顾问感。所有 Agent 都应避免为了差异化而牺牲可用性。

整体视觉应保持节制，核心色调不超过 3 种（标签、状态色、图表辅助色除外），优先用布局、节奏、文案和组件结构表达差异，而不是堆叠复杂配色。

## Anti-references

不要像传统企业后台一样冷硬、拥挤、只有表单和表格。

不要像泛化 SaaS landing 模板一样套用大标题、指标卡、渐变装饰和重复卡片网格。

不要儿童化。即使是寻宝游戏 Agent，也应该有礼物策划和仪式设计的精致感，而不是低龄玩具感。

不要过度赛博、霓虹或 AI 炫技。Agent 的智能应体现在追问质量、结构化输出和行动建议里，而不是视觉噪音里。

不要 Apple 风极简模板。简洁可以，但不能因为留白和圆角而丢掉任务密度、领域信息和决策线索。

## Design Principles

1. 平台复用，Agent 差异化：通用聊天、工具渲染和会话能力应尽量沉淀到底座；每个 Agent 的入口、空状态、问题组织和结果呈现应服务于自身任务。

2. 任务优先于装饰：界面首先帮助用户完成当前 Agent 的核心工作，再考虑视觉记忆点。装饰不能妨碍输入、阅读、选择和结果理解。

3. 可信来自结构：需求诊断类体验要通过清晰步骤、可解释结果和行动路径建立信任；游戏策划类体验要通过场景感、清单、时间线和交付物建立确定性。

4. 控制复杂度：核心色调不超过 3 种，组件语言保持一致，差异化优先来自信息架构、文案语气和领域化组件。

5. 新增 Agent 要低摩擦：设计与代码都应鼓励通过 manifest、prompt、renderer、empty state 等小范围扩展新增 Agent，避免每个 Agent 重新实现整套工作台。

## Accessibility & Inclusion

默认按 WCAG AA 作为设计与实现目标。

交互应支持键盘可用、可见焦点状态、合理的文本对比度和清晰的错误提示。颜色不能作为唯一的信息区分方式，重要状态需要配合文本、图标或结构表达。

动效只用于状态变化、反馈和渐进展示，避免强制性的装饰性动效；对容易造成干扰的动画应支持减少或关闭。

文案应使用清晰中文，避免过度术语化。面向老板和非技术用户的 Agent 要把复杂 AI 概念转译成业务语言；面向生活场景用户的 Agent 要降低策划门槛。
